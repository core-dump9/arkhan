class Vector():
	def __init__(self, tup):
		self._tup = tup;

	def __repr__(self):
			return repr(self._tup);

	"""
	def add(self, vectorb):
		s = Vector(vectorb);
		for i in len(s.iterate()):

		return (x, y, z);
	""