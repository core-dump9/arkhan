import random;
from time import clock;

def funtionvar(x, y):
	return (x+y);

print funtionvar(5, 4);

funtest = lambda x: x+1;

print funtest(17);

ran = random.randint(1, 20);

while 1==1:
	print ran;
